{
  "name": "apigateway",
  "version": "1.0.0",
  "description": "API Gateway with JWT authentication middleware",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  }
}
